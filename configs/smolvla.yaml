CONST:
  EXP_NAME: null
  N_WORKERS: 8
  POLICY_NAME: "smolvla"

DATASET:
  NAME: "lerobot/hzxie/dynamic_objects"
  PIN_MEMORY: false
  DELTA_TIMESTAMPS:
  REQUIRED_FEATURES:
    - "observation.image"
    - "observation.state"
    - "action"

DIR:
  OUTPUT: "./runs"

WANDB:
  ENABLED: false
  PROJECT: "DynamicVLA"
  ENTITY: "haozhexie"
  MODE: "online"
  RUN_ID: null
  LOG_CODE: true
  SYNC_TENSORBOARD: false

TRAIN:
  N_EPOCHS: 500
  BATCH_SIZE: 8
  CKPT_SAVE_FREQ: 100
  OPTIMIZER:
    LR: 1.0e-4
    EPS: 1.0e-8
    WEIGHT_DECAY: 1.0e-10
    BETAS: [0.9, 0.95]
  LR_SCHEDULER:
    NAME: "cosine"
    N_WARMUP_STEPS: 1000

